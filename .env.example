# LLM Tracer Environment Configuration
# Copy this file to .env and adjust values as needed

# ===========================================
# Database Configuration
# ===========================================
# PostgreSQL connection string (recommended for production)
DATABASE_URL=postgresql://llmtracer:llmtracer@localhost:5432/llmtracer

# Or use SQLite for development
# DATABASE_URL=sqlite:///data/llmtracer.db

# PostgreSQL settings (only used by docker-compose.yml)
POSTGRES_USER=llmtracer
POSTGRES_PASSWORD=llmtracer
POSTGRES_DB=llmtracer

# ===========================================
# Server Configuration
# ===========================================
HOST=0.0.0.0
PORT=8000
DEBUG=false

# Change the app port mapping (docker-compose)
APP_PORT=8000

# ===========================================
# Security
# ===========================================
# IMPORTANT: Change this in production!
# Generate with: python -c "import secrets; print(secrets.token_urlsafe(32))"
SECRET_KEY=change-me-in-production-use-a-real-secret-key

# ===========================================
# CORS Configuration
# ===========================================
# Comma-separated list of allowed origins
CORS_ORIGINS=http://localhost:3000,http://localhost:8000

# ===========================================
# SDK Configuration (for client applications)
# ===========================================
# LLM_TRACER_ENDPOINT=http://localhost:8000
# LLM_TRACER_API_KEY=your-api-key
# LLM_TRACER_PROJECT=default
